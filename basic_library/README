# urllib库
    1.python3中只有urllib库，不存在urllib2库（Python2）
    2.urllib是python内置的http请求库，不需要导入即可使用，主要包含四个模块：
        a.request:最基本的HTTP请求模块，用来模拟发送请求
        b.error:异常处理模块
        c.parse:一个工具模块，提供url的处理方法，比如拆分、解析、合并等
        d.robotparser:识别网站的robots.txt，判断可以爬取的模块，使用较少
2.requests库
3.正则表达式
4.抓取猫眼电影排行